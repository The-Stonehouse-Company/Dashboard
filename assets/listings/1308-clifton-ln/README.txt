Listing assets for 1308-clifton-ln

Drop these files here:
  hero.jpg          Front rendering / cover photo (set automatically; no toggle needed)
  one-pager.pdf     PDF one-pager — flip hasOnePager: true in LISTINGS to expose the button
  plans.pdf         Building plans — flip hasPlans: true to expose the button
  design-board.pdf  Design board — flip hasDesignBoard: true to expose the button

After adding files, commit + push the repo so GitHub Pages serves them.
